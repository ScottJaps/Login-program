

%table#volunteerstable
  %thead
    %tr
      %th Name
      %th Phone number / Email
      %th Program
      %th Time In
      %th Time out
    %tbody
    - @the_volunteers.each do |person|
      %tr
        %td= person.name 
        %td= person.phoneNumberEmail
        %td= person.program
        %td= person.time_in.strftime("%I:%M%p")        
        %td= link_to "Sign Out", {:controller => "ifoapp", :action => "sign_person_out", :id => person.id}, :id => "Sign_out_#{person.name}"
